import { ModifyDashDirective } from './modify-dash.directive';

describe('ModifyDashDirective', () => {
  it('should create an instance', () => {
    const directive = new ModifyDashDirective();
    expect(directive).toBeTruthy();
  });
});
